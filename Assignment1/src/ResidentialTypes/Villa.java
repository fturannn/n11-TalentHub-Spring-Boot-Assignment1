package ResidentialTypes;

public class Villa extends ResidentialModel{
    public Villa(int price, int area, int numOfRooms, int numOfHalls, String name) {
        super(price, area, numOfRooms, numOfHalls, name);
    }
}
